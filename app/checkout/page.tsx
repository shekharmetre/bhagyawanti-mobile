import { CheckoutPage } from '@/components/checkout/checkout-page';

const Checkout = async () => {
  // console.log(process.env.NEXT_PUBLIC_BASE_URL,"give e ")
  return (
    <CheckoutPage />
  )
};

export default Checkout;
